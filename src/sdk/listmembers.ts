/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { crmListMembersList } from "../funcs/crmListMembersList.js";
import { ClientSDK, RequestOptions } from "../lib/sdks.js";
import * as operations from "../models/operations/index.js";
import { PageIterator, unwrapResultIterator } from "../types/operations.js";

export class ListMembers extends ClientSDK {
  /**
   * List list members
   *
   * @remarks
   * Retrieve the records that belong to a List. Returns a paginated set of members, each carrying the downstream record `id` and its `object_type`. Use this to enumerate the contents of a List (for example the contacts or companies a HubSpot list contains) instead of dropping to the proxy.
   */
  async list(
    request: operations.CrmListMembersAllRequest,
    options?: RequestOptions,
  ): Promise<
    PageIterator<operations.CrmListMembersAllResponse, { cursor: string }>
  > {
    return unwrapResultIterator(crmListMembersList(
      this,
      request,
      options,
    ));
  }
}
